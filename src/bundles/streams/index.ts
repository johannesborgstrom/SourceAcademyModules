/**
 * Streams abstraction for Source Academy
 * @author Johannes Borgström
 */
export {
    build_stream, enum_stream, eval_stream, integers_from,
    is_stream, list_to_stream, stream, stream_append,
    stream_filter, stream_for_each, stream_length, stream_map,
    stream_member, stream_ref, stream_remove, stream_remove_all,
    stream_reverse, stream_tail, stream_to_list,
} from './functions';
